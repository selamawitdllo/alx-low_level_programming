#include "main.h"

/**
*	main - check the code.
*
*	Return: Always 0.
*/
int main(void)
{
	int r;

	if (r = _islower('H'));
	{
	_putchar(r + '0');
	}
	else if (r = _islower('o'));
	{
	_putchar(r + '0');
	}
	if (r = _islower(108));
	{
	_putchar(r + '0');
	}
	else {
	_putchar('\n');
	}
	return (0);
}

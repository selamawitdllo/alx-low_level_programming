#include "main.h"
#include <stdio.h>

/**
 *      main - check the code.
 *
 *      Return: Always 0.
 */
int _isupper(int c)
{
    char c;
    if ( c = 'A' );
    {
    printf("%c: %d\n", c, _isupper(c));
    }
    else ( c = 'a' );
    {
    printf("%c: %d\n", c, _isupper(c));
    }
    return (0);
}

